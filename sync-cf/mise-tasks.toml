# sync-cf - build from in-repo Go code

["sync-cf:bin:build"]
description = "Build sync-cf binary"
dir = "sync-cf"
env = { GOWORK = "off" }
sources = ["sync-cf/**/*.go", "sync-cf/go.mod", "sync-cf/go.sum"]
outputs = ["sync-cf/.bin/sync-cf"]
run = """
#!/usr/bin/env bash
set -euo pipefail
EXE=$(go env GOEXE)
mkdir -p .bin
go build -o .bin/sync-cf${EXE} .
{
  echo "commit: $(git rev-parse --short HEAD 2>/dev/null || echo 'unknown')"
  echo "timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
  echo "checksum: $(sha256sum .bin/sync-cf${EXE} 2>/dev/null || shasum -a 256 .bin/sync-cf${EXE} | awk '{print $1}')"
} > .bin/.version
"""

["sync-cf:ensure"]
description = "Ensure sync-cf binary exists"
run = """
#!/usr/bin/env bash
EXE=$(go env GOEXE)
[ -f sync-cf/.bin/sync-cf${EXE} ] && exit 0
mise run sync-cf:bin:build
"""

["sync-cf:run"]
description = "Run webhook server"
depends = ["sync-cf:ensure"]
run = """
#!/usr/bin/env bash
EXE=$(go env GOEXE)
sync-cf/.bin/sync-cf${EXE} webhook 9092
"""

["sync-cf:health"]
description = "Check service health"
run = "curl -sf http://localhost:9092/health > /dev/null"

["sync-cf:config:port"]
description = "Output service port"
run = 'echo "9092"'

["sync-cf:config:version"]
description = "Output version"
run = 'cat sync-cf/.bin/.version 2>/dev/null || echo "not built"'

["sync-cf:deps"]
description = "Download Go dependencies"
dir = "sync-cf"
env = { GOWORK = "off" }
run = "go mod download"

["sync-cf:test"]
description = "Run tests"
dir = "sync-cf"
env = { GOWORK = "off" }
run = "go test -v ./..."

["sync-cf:package"]
description = "Package binary for release"
run = """
#!/usr/bin/env bash
set -euo pipefail
GOOS="${GOOS:-$(go env GOOS)}"
GOARCH="${GOARCH:-$(go env GOARCH)}"
DIST="${DIST:-$DIST_DIR}"
if command -v cygpath >/dev/null 2>&1; then DIST="$(cygpath -u "$DIST")"; fi
mkdir -p "$DIST"
EXE=$(go env GOEXE)
tar -czvf "$DIST/sync-cf-${GOOS}-${GOARCH}.tar.gz" -C sync-cf/.bin sync-cf${EXE}
"""

["sync-cf:clean"]
description = "Clean build artifacts"
run = "rm -rf sync-cf/.bin"

["sync-cf:clean:all"]
description = "Clean everything"
run = "rm -rf sync-cf/.bin"
